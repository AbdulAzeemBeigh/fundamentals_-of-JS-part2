'use strict'

let male = false;
const educated = true;

if(educated) males = true;  // variable name wrong strict mode help
if(male) console.log("I can drive");

const interface = 'audio';
const private = 'video';
const if = 223;
console.log(interface,private,if);